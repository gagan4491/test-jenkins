pipeline {
    agent { label 'master' }
    stages {
        stage('build') {
            steps {
                echo "Hello World!"
                sh 'printenv'
                sh 'pwd'
                echo "test2"
                
        }
    }
}
}
