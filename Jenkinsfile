pipeline {
    agent { label 'master' }
    
    parameters {
       (
      [extendedChoice(description: 'Test Suites', multiSelectDelimiter: ',', name: 'Masspay', quoteValue: true, type: 'PT_CHECKBOX', visibleItemCount: 5)]
       )     
      }
    stages {	    
               stage('pre build') {
            steps {
                cleanWs()
        }
                
                
        }
		
		}
		
		}
