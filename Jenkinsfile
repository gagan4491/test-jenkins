pipeline {
    agent { label 'master' }
    stages {
                stage('build') {
            steps {
                echo "Hello World!guys"
                sh 'printenv'
                sh 'pwd'
                echo "done "
                
        }
    }
}
}
